# secrethound 기획서

## 개요
Git 레포지토리(현재 파일 + 전체 커밋 히스토리)에서 유출된 API 키, 토큰, 인증정보 등 민감 정보를 자동으로 탐지하는 시크릿 스캐너.

**탐지 범위**: 머신 크리덴셜(API 키·토큰·개인키·DB 접속정보)에 한정한다. 주민등록번호·카드번호 등
개인정보(PII)는 성격이 다른 DLP 영역이므로 v0.4의 옵트인 확장 룰셋으로 분리한다.

## 목적
- 개발자가 실수로 커밋한 시크릿을 배포/공개 전에 미리 찾아내는 도구 제작
- 보안 도메인 지식(탐지 로직 설계)과 실제 동작하는 결과물을 함께 보여주는 포트폴리오

## 핵심 기능
1. **패턴 매칭** — AWS/Slack/Stripe 등 형식이 정해진 시크릿을 정규식으로 탐지
2. **엔트로피 분석** — 형식이 없는 랜덤 문자열을 Shannon entropy 기반으로 탐지
3. **Git 히스토리 스캔** — 현재 파일뿐 아니라 과거 커밋에 남아있는 시크릿까지 탐지
4. **오탐 필터링** — 테스트/예시 코드 등 컨텍스트를 고려해 false positive 감소

## 결과물 형태
- CLI 도구 (`secrethound scan ./repo`)
- (선택) 웹 대시보드 — 스캔 결과 시각화
- (선택) GitHub Actions 연동 — PR에서 시크릿 발견 시 CI 실패 처리

## MVP 범위

"동작하는 시크릿 스캐너 하나"를 먼저 완성하고, 근거·편의 기능은 그 뒤에 붙인다.
MVP의 기준은 **임의의 git 레포에 대해 CLI 한 줄로 현재 파일 + 전체 히스토리를 스캔하고
사람이 읽을 수 있는 리포트를 내놓는 것**이다.

### MVP 포함 (v0.1)

| 기능 | 범위 | 상태 |
|---|---|:---:|
| 패턴 매칭 | YAML 룰셋(32개 룰) 기반 정규식 탐지, 키워드 프리필터, 룰별 엔트로피 게이트 | ✅ |
| Repo Collector | 파일 순회, 제외 규칙, 바이너리 판별, 심볼릭 링크 스킵 | ✅ |
| Git 히스토리 스캔 | `git log -p` 스트리밍 파싱, 커밋 해시/작성자/날짜 기록 | ✅ |
| FP Filter | 경로/값 기반 신뢰도 감점, builtin 규칙, allowlist, 임계값 컷 | ✅ |
| Reporter | 심각도순 정렬, 요약 통계, 텍스트/JSON 출력 | ✅ |
| CLI | `scan` 커맨드, 룰셋/포맷/출력 플래그, 종료 코드 분리 | ✅ |

### MVP 제외

| 제외 항목 | 사유 |
|---|---|
| Entropy Analyzer(독립 탐지기) | 단독 엔트로피 탐지는 해시·UUID·base64 등이 전부 걸려 오탐이 급증한다. 임계값 튜닝 비용 대비 얻는 게 적어, 정규식 매칭의 **보조 검증**으로만 쓴다. |
| 웹 대시보드 | Reporter의 JSON을 소비하는 구조라 코어와 결합도가 없다. 언제 붙여도 무방하므로 뒤로 미룬다. |
| GitHub Actions 연동 | 종료 코드 규약(0/1/2)만 고정되면 붙는 얇은 층. MVP 직후 작업. |
| 키 유효성 검증 / 벤치마크 / baseline / 병렬 스캔 / remediation | 확장 단계 항목. 아래 릴리스 단계 참조. |
| PII(개인정보) 탐지 | 현재 룰셋은 전부 머신 크리덴셜이다. PII 탐지는 DLP라는 별개 카테고리라 지금 섞으면 도구의 정체성과 오탐 기준이 흐려진다. 체크섬 validator 훅이 선행돼야 하므로 v0.4로 미룬다. |

### MVP 완료 기준 (Definition of Done)

- [x] 임의의 git 레포에 대해 `secrethound scan ./repo --history` 가 동작한다
- [x] 탐지 시 종료 코드 1, 실행 실패 시 2로 분리된다
- [x] `--format json` 출력 스키마가 고정되어 있다 (대시보드/CI가 공통으로 소비)
- [x] 각 패키지에 단위 테스트가 있고 통과한다
- [x] README에 설치·사용법·룰셋 작성법이 정리되어 있다

### 릴리스 단계

| 단계 | 내용 | 목적 |
|---|---|---|
| **v0.1 — MVP** | 위 "MVP 포함" 전부 + README | 동작하는 결과물 확보 |
| **v0.2 — 실사용** | GitHub Actions 연동, baseline 파일, 병렬 스캔 | 실제 프로젝트에 도입 가능한 수준 |
| **v0.3 — 근거** | 키 유효성 검증, 평가 하네스 + gitleaks/trufflehog 벤치마크 | 아래 차별화 포인트를 **숫자로 증명** |
| **v0.4 — 선택** | 웹 대시보드, remediation 안내, PII 탐지 룰셋(옵트인) | 보여주기·사후 대응·탐지 범위 확장 |

**첫 배포는 `v0.4.0`** (2026-09-08). v0.1~v0.4의 항목 중 PII 탐지만 남기고 전부
구현된 상태에서 태그를 잘랐다. 여기에 로드맵에 없던 항목 하나가 더 들어갔는데,
피드백을 받아 만든 **일반 사용자용 진입점**(터미널·Go 설치 없이 아이콘 더블클릭으로
실행)이다. 이 도구의 대상이 "실수로 키를 커밋하는 사람"인 이상, CLI만 있는 상태로는
정작 그 사람에게 닿지 않는다는 지적이었다. 자세한 내용은 [progress.md](./progress.md) 참조.

각 항목의 예상 소요와 세부 체크리스트는 [progress.md](./progress.md) 참조.

## 차별화 포인트
- gitleaks/trufflehog 대비 오탐 감소 로직(파일 경로 컨텍스트 가중치 등)
- 결과를 보기 쉽게 보여주는 대시보드 UI

## 아키텍처

### 전체 흐름
```
[대상 레포 경로/URL]
        │
        ▼
┌─────────────────────┐
│ 1. Repo Collector    │  현재 파일 + git log -p 로 커밋 히스토리 전체 diff 수집
└─────────────────────┘
        │
        ▼
┌─────────────────────┐
│ 2. Detection Engine   │
│  ├─ Regex Matcher     │  AWS/Slack/Stripe/JWT 등 알려진 포맷 매칭
│  └─ Entropy Analyzer  │  임의 문자열의 Shannon entropy 계산 후 임계값 초과 시 후보로 등록
└─────────────────────┘
        │
        ▼
┌─────────────────────┐
│ 3. FP Filter          │  파일 경로(test/, example/), 변수명, 화이트리스트 규칙으로 오탐 제거
└─────────────────────┘
        │
        ▼
┌─────────────────────┐
│ 4. Reporter           │  탐지 결과를 파일:라인, 종류, 심각도로 정리
└─────────────────────┘
        │
        ├──▶ CLI 출력 (터미널 리포트)
        ├──▶ 웹 대시보드 (JSON → 시각화)
        └──▶ GitHub Actions (탐지 시 CI 실패 처리)
```

### 컴포넌트별 역할
| 컴포넌트 | 역할 | 비고 |
|---|---|---|
| Repo Collector | 현재 워킹 디렉토리 파일 + git 커밋 히스토리(diff) 수집 | 히스토리에서 지워진 시크릿도 잡아야 하므로 필수 |
| Regex Matcher | 알려진 시크릿 포맷을 정규식 룰셋으로 매칭 | 룰셋은 별도 설정 파일(yaml/json)로 분리해 확장 가능하게 |
| Entropy Analyzer | 정규식으로 못 잡는 랜덤 문자열 후보 탐지 | 임계값 튜닝이 오탐률에 직결 |
| FP Filter | 컨텍스트 기반으로 오탐 후보 제거 | 프로젝트 차별화 핵심 로직 |
| Reporter | 탐지 결과를 표준 포맷(JSON)으로 정리 | CLI/대시보드/CI가 공통으로 소비 |

### 동작 방식 (요약)
1. 사용자가 CLI로 스캔 대상 경로 지정
2. Repo Collector가 현재 파일 스냅샷 + 전체 git 히스토리 diff를 순회 가능한 형태로 수집
3. 각 텍스트 라인에 대해 Regex Matcher와 Entropy Analyzer를 동시에 적용해 후보 탐지
4. FP Filter가 컨텍스트(파일 경로, 주변 코드)를 보고 후보의 신뢰도 점수를 조정, 임계값 미달 항목 제외
5. Reporter가 최종 결과를 정리해 CLI 출력 / 대시보드용 JSON / CI 종료 코드로 내보냄

## 기술 스택
- **언어: Go**
  - 단일 실행 바이너리로 배포 가능 (설치 과정 없이 CLI 배포)
  - goroutine으로 파일/커밋 병렬 스캔에 유리
  - 정규식(`regexp`), 파일 순회, `os/exec` 기반 git 연동 모두 표준 라이브러리로 해결
- **CLI 프레임워크**: `spf13/cobra` (서브커맨드 구조, 플래그 파싱)
- **룰셋 설정**: YAML (`gopkg.in/yaml.v3`)
- **Git 연동**: `git log -p` 직접 호출 (`os/exec`)
  - go-git 대신 선택한 이유: 스캔 대상이 git 레포인 이상 git은 이미 설치되어 있고,
    대형 레포에서 diff 순회 성능이 순수 Go 구현보다 앞선다. 출력을 스트리밍으로 읽어
    메모리 사용량도 레포 크기와 무관하게 일정하다.
- **웹 대시보드**: TBD (Reporter의 JSON 출력을 소비하는 구조라 나중에 결정해도 무방)
