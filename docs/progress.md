# progress

> 예상 기간은 모두 **하루 3~4시간 작업 기준**의 "작업일"이다.

## 다음 할 일

지금 바로 할 수 있는 작업은 없다. 기획서 범위와 확장 로드맵(아래 참조)은 전부
끝났고, 남은 두 가지는 외부 조건이 갖춰져야 착수할 수 있어 대기 중이다.

| 항목 | 막힌 이유 |
|---|---|
| 제3자 코퍼스로 재측정 | 편향 없는 코퍼스를 아직 구하지 못함 — 현재 벤치마크의 편향을 없애는 유일한 방법 |
| 검증기 나머지 10곳 성공 경로 확인 | Stripe·Shopify 등 사업자 인증·유료 계정 필요. 기회 될 때마다 |

## 작업 순서

| 작업 | 상태 |
|------|:---:|
| 기술 스택 확정 | ✅ 완료 |
| 시크릿 탐지 룰셋 정의 (`rules/default.yaml`, 37개 룰) | ✅ 완료 |
| 프로젝트 구조 세팅 + 룰셋 로더 | ✅ 완료 |
| 파일 수집기(Repo Collector) 구현 | ✅ 완료 |
| 정규식 기반 패턴 매칭 엔진 구현 | ✅ 완료 |
| Git 히스토리 스캔 기능 구현 | ✅ 완료 |
| 오탐 필터링 로직 구현 | ✅ 완료 |
| Reporter (CLI 출력 / JSON) 구현 | ✅ 완료 |
| CLI 인터페이스 완성 | ✅ 완료 |
| README / 문서 정리 | ✅ 완료 |
| GitHub Actions 연동 (선택) | ✅ 완료 |
| 웹 대시보드 (선택) | ✅ 완료 |
| **[확장] 키 유효성 검증** | ✅ 완료 |
| **[확장] 평가 하네스 + 벤치마크** | ✅ 완료 |
| **[확장] baseline 파일** | ✅ 완료 |
| **[확장] 병렬 스캔 + 성능 수치** | ✅ 완료 |
| **[확장] remediation 안내** | ✅ 완료 |

<!-- 엔트로피 분석 로직, PII 탐지 룰셋은 착수하지 않기로 결정/보류된 항목이라 표에서 뺐다.
     상세 사유와 나중에 재검토할 경우의 설계 메모는 "MVP 제외 (이후 검토)" 절 참조. -->
<!-- 상태 표시: ⬜ 미완료 / 🔄 진행중 / ✅ 완료 -->

## 현재 상태 (2026-09-08)

- 기획서의 4개 컴포넌트(Repo Collector / Detection Engine / FP Filter / Reporter) **전부 구현 완료**
- MVP CLI 동작 (`scan` 커맨드, 종료 코드 분리, 텍스트/JSON 출력, `--history`, `--report`,
  `--baseline` / `--baseline-out`, `--workers`)
- 탐지 룰 37개(해외 29 + 국내 8), **전부 평가 코퍼스로 검증됨** (커버리지 37/37)
- 정확도 P 1.000 / R 1.000 / F1 1.000 (자체 코퍼스 기준, 과적합 전제)
- gitleaks v8.30.1 비교 완료 (동일 코퍼스 기준 0.875)
- 실제 오픈소스 레포 5개(13,294 커밋) 스캔 검증 — 오탐 원인 2가지
- GitHub Actions(`action.yml`) + CI 워크플로 구성, 원격 푸시 완료
- **키 유효성 검증(`--validate`) 구현 완료** — 37개 룰 중 17개, 발급처 14곳,
  무료 발급 6곳은 실제 키로 성공 경로까지 확인
- **baseline 파일 구현 완료** — 기존 시크릿 도입 레포 지원
- **병렬 스캔 구현 완료** — 정규식 매칭을 워커 풀로 분산. 실측 결과 히스토리
  스캔은 `git log` 자체가 병목이라 체감 효과는 크지 않음(정직하게 기록해 둠)
- **remediation 안내 구현 완료** — 룰별 폐기 절차(37개 룰 전부)와 히스토리
  발견 시 `git filter-repo` 제거 명령을 리포트 "대응 방법" 절에 자동 표시
- **웹 대시보드 구현 완료** — `web/dashboard.html` 단일 정적 파일. 빌드·서버 없이
  브라우저에서 JSON 리포트를 로드해 테이블로 보여줌
- **국내 서비스 룰셋 추가 완료 (2026-09-08)** — 공공데이터포털·카카오·네이버·NCP·
  토스페이먼츠·포트원 8개 룰. 검증기는 아직 없어 검증기 커버리지 분모만 늘었다
  (17/29 → 17/37). 상세는 아래 "국내 서비스 룰셋" 절 참조

## MVP

기획서가 정의한 MVP 범위(패턴 매칭, Repo Collector, Git 히스토리 스캔, FP Filter,
Reporter, CLI)는 전부 구현·검증됐다. Definition of Done 체크리스트와 릴리스
단계 구분은 [기획서.md](./기획서.md)의 "MVP 범위" 절 참조 — 중복을 피하기 위해
여기서는 반복하지 않는다.

## MVP 제외 (이후 검토)

### Entropy Analyzer — 독립 탐지기
정규식 룰에 없는 미지의 시크릿을 엔트로피만으로 탐지하는 기능. **MVP에서 제외한다.**
- 제외 이유: 단독 엔트로피 탐지는 오탐이 급증해(해시값, UUID, base64 인코딩된 이미지,
  압축된 문자열 등이 전부 걸림) 임계값 튜닝에 드는 비용 대비 얻는 게 적다.
  현재는 룰별 `entropy` 임계값을 정규식 매칭의 **보조 검증**으로만 쓰는 편이 정확도가 높다.
- 이미 구현된 것: `detector.Shannon()` (룰별 엔트로피 게이트에서 사용 중)
- 나중에 한다면: base64/hex 문자셋 판별 → 문자셋별 임계값 → 정규식 룰과 중복 제거 순서

### PII(개인정보) 탐지 — 확장 룰셋
주민등록번호·전화번호·카드번호 등 **사람에 대한 정보**를 탐지하는 기능. **MVP에서 제외한다.**

- 현재 상태: `rules/default.yaml`의 32개 룰은 전부 머신 크리덴셜(API 키/토큰/개인키/DB 접속정보)이다.
  PII 룰은 하나도 없다.
- 제외 이유: MVP의 정의가 "시크릿 스캐너"이고 동종 도구(gitleaks/trufflehog)도 credential 전용이다.
  PII 탐지는 DLP라는 별개 카테고리라, 지금 섞으면 도구의 정체성과 오탐 기준이 함께 흐려진다.
- 붙이기 쉬운 이유: 룰이 YAML로 분리돼 있어 `config.Rule` 구조체를 건드리지 않고 정규식 룰만
  추가해도 동작한다. 전화번호·이메일·계좌번호 등은 정규식만으로 충분하다.

**선행 작업 2가지** (이게 없으면 오탐 때문에 못 쓴다)

1. **체크섬 validator 훅** — 주민등록번호(가중치 검증), 카드번호(Luhn)는 정규식만으로는 오탐이 심하고
   Go의 RE2는 백레퍼런스가 없어 정규식 안에서 해결이 불가능하다. `Penalty.Builtin`과 같은 방식으로
   `Rule`에 `validator` 필드를 추가해 코드 검증을 연결해야 한다.
2. **엔트로피 게이트 미적용** — PII는 랜덤 문자열이 아니라 엔트로피가 낮다. 룰의 `entropy` 필드를
   비우면 되지만 그만큼 FP Filter 부담이 커지므로, 더미 번호(`000-0000-0000`, `123-45-67890` 류)와
   테스트 픽스처 경로에 대한 감점 룰을 함께 넣어야 한다.

**설계 메모**

- 룰을 `pii` 태그로 묶고 `--tags pii` 로 옵트인. **기본 비활성**이 맞다.
  (크리덴셜 스캔만 원하는 사용자에게 PII 오탐을 강제로 보여줄 이유가 없다)
- 마스킹은 필수. 스캔 리포트에 주민번호가 그대로 찍히면 **리포트 자체가 유출 경로**가 된다.
  기존 `detector`의 마스킹 로직을 재사용하되 PII는 마스킹 비율을 더 높게 잡는다.

**체크리스트**

- [ ] `config.Rule`에 `validator` 필드 추가 + 검증 코드 구현 (`luhn`, `kr-rrn`)
- [ ] `rules/pii.yaml` 작성 — 주민등록번호, 휴대폰번호, 카드번호, 계좌번호, 이메일, 여권번호
- [ ] `--tags` / `--exclude-tags` 플래그로 룰 그룹 선택
- [ ] PII 전용 감점 룰 (더미 번호 패턴, 테스트 픽스처 경로)
- [ ] PII 강화 마스킹 + 리포트 출력 시 원본 노출 여부 점검

### 이후 개선 아이디어
- [ ] FP Filter가 탐지된 값뿐 아니라 **주변 라인 컨텍스트**(변수명 등)도 보도록 확장
      — `const placeholder = "..."` 처럼 값이 아닌 변수명에 단서가 있는 경우 대응
- [ ] `--show-filtered` 플래그로 걸러진 항목과 감점 사유 출력 (임계값 튜닝용)
- [ ] `--exclude` 플래그로 스캔 제외 경로 지정
      — 현재는 제외하려면 룰셋 전체를 복사해야 한다. 이 레포 자신도 평가 코퍼스 때문에
      자기 스캔이 불가능하다
- [ ] 벤더 특정 룰이 **키 이름과 값이 다른 줄에 있을 때** 매칭되지 않는 문제
      (`twilio: { apiKey: "SK..." }`). 줄 단위 스캔의 구조적 한계라 컨텍스트 윈도우가 필요하다
- [ ] 문서/예제 경로(`docs/`)의 예시 시크릿 처리 재검토
      — 실측에서 Flask 문서의 예시 SECRET_KEY가 신뢰도 60으로 살아남았다
- [ ] 히스토리 스캔이 실질적으로 느려지는 대형 레포가 생기면, 워커 풀보다
      git 호출 방식 자체(파일별 병렬 `git log` 등)를 재검토
- [ ] `generic-api-key`의 키워드 목록이 `token`/`secret`/`license`/`pat`를 단독으로는
      받지 않는다(`access_token`·`auth_token`·`secret_key`·`client_secret` 형태만).
      Okta `API_TOKEN`, Plaid `SECRET`, Notion·Vercel·HubSpot의 단독 `TOKEN`이 이 밖에
      있다. 2026-09-09 실측: 룰 없는 국내외 서비스 27개 중 4개만 이 룰로 회복,
      나머지 23개는 이 키워드 제한 때문에 여전히 미탐. 의도적으로 좁힌 경계라
      건드리지 않았다 — 넓히면 CSRF 토큰·세션 ID 같은 비시크릿 필드까지 잡힐
      위험이 있어, 코퍼스에 그런 트랩 케이스를 먼저 마련한 뒤 다뤄야 한다
- [ ] 룰에 아예 없는 서비스는 구조적으로 미탐이다(엔트로피 단독 탐지가 MVP 제외라
      최후 방어선이 없음). 2026-09-09 프로브에서 확인된 미등록 국내 서비스:
      한국투자증권 KIS, 쿠팡파트너스, 나이스페이, KG이니시스, 솔라피, 카카오페이.
      해외는 Cloudflare·Supabase·DigitalOcean 등 다수. 요청이 오면 그때그때
      룰을 추가하는 것으로 충분한지, 별도로 우선순위를 매길지는 미정

## 확장 로드맵 (차별화 근거 만들기)

**문제 인식**: 기획서의 차별화 포인트는 *"gitleaks/trufflehog 대비 오탐 감소"* 인데,
현재 그 주장을 **증명하는 근거가 없다**. FP Filter에 감점 룰을 넣었다는 사실만으로는
"실제로 오탐이 줄었는가"에 답할 수 없다. 분량이 부족한 게 아니라 주장에 근거가 비어 있는 상태.
아래는 깊이 대비 효율 순으로 정리한 확장 항목이다.

| 우선순위 | 항목 | 예상 | 목적 | 상태 |
|:---:|---|---|---|:---:|
| 1 | 키 유효성 검증 (Validator) | 2~3일 | 추정이 아니라 발급처 응답으로 위험 여부 판정 | ✅ |
| 2 | 평가 하네스 + 벤치마크 | 2일 | 차별화 주장에 숫자를 붙임 | ✅ |
| 3 | baseline 파일 | 1일 | 실무 도입 필수 기능 | ✅ |
| 4 | 병렬 스캔 + 성능 수치 | 1~2일 | Go 선택 이유(goroutine)의 정당화 | ✅ |
| 5 | remediation 안내 | 1일 | 탐지 이후 행동까지 연결 | ✅ |

**1 + 2만 붙여도 3~4주짜리 프로젝트가 되고, 성격이 "만들어봤다"에서
"기존 도구의 약점을 측정하고 개선했다"로 바뀐다.**

#### 1. 키 유효성 검증 (Validator) — 완료

**문제였던 것**: 탐지된 키를 실제 API에 최소 권한으로 호출해 살아있는지 확인하는
기능인데, 처음 구현할 때 발급처의 실제 거부 응답 형태를 관찰하지 않고 상태코드만
보고 판정했다. 14곳 중 대다수가 공통 `classify`를 쓰고 `401 = 폐기됨`으로 판정한
것인데, 요청 자체가 잘못 만들어져 있어도(헤더 이름 오타, API 버전 누락 등) 발급처는
살아있는 키에 똑같이 401을 주므로, 그 키가 "폐기됨"으로 보고돼도 아무도 눈치채지
못하는 상태였다.

- [x] 탐지된 키를 실제 API에 **최소 권한**으로 호출해 살아있는지 확인
      (AWS `sts:GetCallerIdentity`, GitHub `GET /user`, Slack `auth.test` 등
      29개 룰 중 17개, 발급처 14곳)
- [x] 결과를 `유효 / 폐기됨 / 검증불가` 3단계로 Finding에 기록
- [x] 네트워크 호출이므로 기본 비활성 + `--validate` 플래그로 옵트인
- [x] 타임아웃/레이트리밋/오프라인 환경 처리
- [x] AWS SigV4 직접 구현 + Access Key ID ↔ Secret Access Key 짝짓기
- [x] 리포트에 키 값이 새지 않도록 에러 문자열 redact
- [x] **(1) 실패 시그니처 검사** — `classify` → `classifyWith(sig, ...)`로 교체.
      401이어도 본문이 발급처의 실제 거부 형식과 다르면 `폐기됨`이 아니라
      `검증불가 (검증기 점검 필요)`로 남긴다
- [x] **(2) 셀프체크** — `secrethound selfcheck` 추가. 형식만 맞는 가짜 키를
      14곳에 보내 표식이 여전히 유효한지 확인한다. **진짜 자격증명 불필요**.
      매주 월요일 CI([selfcheck.yml](../.github/workflows/selfcheck.yml))로 자동 실행

**실제로 발견한 것**: 셀프체크를 처음 돌려보니 14곳 **전부** 시그니처가 비어 있어
`[미확인]`이었다. 실제 API로 관찰한 응답으로 채우자 **AWS가 401이 아니라
403(`InvalidClientTokenId`)을 준다**는 것이 드러났다 — 문서만 보고 짐작했다면
놓쳤을 부분이고, 이게 정확히 이 항목이 막으려던 종류의 결함이다. 채운 뒤에는
14곳 전부 `[정상]`.

**착수 전 가정이 틀렸던 부분** — 원래 이 항목의 의의를 *"죽은 키를 걸러내는
반박 불가능한 오탐 제거"* 라고 적어놨는데, 구현하며 그게 성립하지 않는다는 걸 알았다.

폐기된 진짜 키와 **애초에 키가 아니었던 문자열은 발급처 입장에서 똑같이 401**이다.
응답만으로 이 둘을 구분할 수 없으므로 "폐기됨"은 오탐 증거가 될 수 없다.
증명되는 것은 "지금은 악용 불가"뿐이다. 게다가 히스토리에 남은 폐기 키는 여전히
"이 레포는 시크릿을 커밋한 적이 있다"는 사실을 말해주므로 리포트에서 지워서도 안 된다.

그래서 검증 결과가 신뢰도 점수를 건드리지 않도록 설계를 바꿨다.
신뢰도는 "탐지가 맞았는가", 검증은 "지금 위험한가"라는 **다른 축**이고,
하나로 합치면 폐기된 진짜 유출이 리포트에서 사라진다.
진짜 오탐 제거 효과가 있는 것은 `유효` 한 방향뿐이다.

**성공 경로 검증 — 완료 (2026-09-08)**: 무료 발급 가능한 6곳(GitHub·GitLab·
Slack·Discord·Telegram·npm) 전부를 실제 키로 확인했다.

1차 시도에서 GitHub·npm·Telegram 3곳은 바로 `유효`로 확인됐다. 나머지 3곳은
탐지 단계에서부터 걸러져 검증이 시도되지 못했는데, 원인이 서로 달랐다.

| 발급처 | 1차 실패 원인 | 결과 |
|---|---|---|
| GitLab | 최근 PAT가 `glpat-xxx.xx.xxx` 형태로 마침표를 포함하는데 룰의 문자 클래스에 마침표가 없어 통째로 안 잡힘 | **룰 버그로 확인, 수정 완료** (아래 참조) |
| Slack | 처음 붙여넣은 값에 `xoxb-` 접두사가 없었음 (Bot Token이 아닌 다른 값을 복사한 것으로 추정) | 정확한 위치(OAuth & Permissions → Bot User OAuth Token)를 안내해 재발급 → `유효` 확인 |
| npm | 64자리 hex 값으로 `npm_` 접두사가 없었음 (Granular 토큰이나 다른 화면의 값으로 추정) | Classic Token으로 재발급 지정 → `유효` 확인 |

Slack·npm은 결국 **룰 결함이 아니라 처음에 잘못된 값을 복사한 것**으로 판명됐다.
GitLab만 실제 룰 버그였다. 6곳 모두 확인 직후 폐기했다.

**GitLab PAT 룰 버그 수정 상세**: 2024년 이후 발급되는 토큰이
`glpat-<본문>.<2자 체크섬>.<나머지>` 형태로 마침표를 포함하는데, 기존 정규식은
마침표를 허용하지 않아 **통째로 탐지되지 않았다.** 오탐 필터 이전 단계에서 이미
놓치는 것이라 지금까지 나온 결함 중 가장 심각한 축에 속했다.

- [x] 정규식을 `glpat-[A-Za-z0-9\-_]{20,}(?:\.[A-Za-z0-9\-_]{2}\.[A-Za-z0-9\-_]+)?`
      로 수정 — 구형식(마침표 없는 20자)과 신형식을 모두 매칭
- [x] eval 코퍼스에 신형식 케이스 추가(`config/services.env`) — 실측이 아니었으면
      발견 못 했을 결함이라 회귀 테스트로 고정. F1 1.000·커버리지 29/29 유지

아직 확인 안 된 나머지 10곳(AWS 제외)은 사업자 인증·유료 계정이 필요해
개인 프로젝트 범위를 벗어나므로 README에 "거부 경로만 확인됨"으로 명시해 두었다.

**아직 안 된 것** — 위 10곳의 성공 경로(2xx → `유효`)는 여전히 미확인이다.
셀프체크는 가짜 키의 거부 경로만 검증하므로, 진짜 키가 있어야 확인된다.

**AWS 임시 자격증명(세션 토큰) 검증 추가 (2026-09-08)**: `aws-session-token`
룰은 원래 `aws-access-key-id`·`aws-secret-access-key`처럼 별도로 카운트되는
룰이 아니라 검증의 "재료"로만 쓰이므로, 이 작업이 **17/29라는 숫자 자체를
움직이지는 않는다** — 정직하게 밝혀두는 부분이다. 대신 이미 카운트돼 있던
`aws-access-key-id`의 실질적인 성공률을 높인다.

기존에는 임시 자격증명(`ASIA`로 시작하는 키)을 발견하면 세션 토큰이 있는지
확인도 안 하고 무조건 "STS 임시 자격증명이라 세션 토큰 없이는 검증할 수
없음"으로 포기했다 — 실제로는 `.env` 파일처럼 세 값(Access Key ID·Secret·
세션 토큰)이 나란히 유출되는 경우가 드물지 않은데도 그랬다.

- [x] `pairAWSKeys`가 Secret Access Key뿐 아니라 근처의 `aws-session-token`
      값까지 찾도록 확장 (`internal/validator/aws.go`)
- [x] `X-Amz-Security-Token` 헤더를 요청에 싣고 **SignedHeaders에도 포함**해
      서명 — AWS 문서는 서비스마다 다를 수 있다고 명시하지만, botocore를 포함한
      참조 구현들이 서명 대상에 포함시키는 쪽을 따른다 (서명이 틀리면 살아있는
      키도 SignatureDoesNotMatch로 조용히 "검증불가" 처리되므로 검증된 쪽을 택함)
- [x] 세션 토큰 값도 `redact()` 대상에 포함 — 에러 문자열에 섞여 나가지 않도록
- [x] 실제 AWS STS에 합성 임시 자격증명(ASIA + 무작위 Secret/세션 토큰)을
      보내 스모크 테스트 — `InvalidClientTokenId`로 정확히 `폐기됨` 분류됨을
      확인. 서명이 틀렸다면 `SignatureDoesNotMatch`(검증불가)가 나왔을
      것이므로, 이 결과 자체가 세션 토큰 포함 서명이 올바르다는 증거다

#### 2. 평가 하네스 + 벤치마크 — 완료
- [x] 시크릿을 의도적으로 심은 테스트 코퍼스 구성 (진짜 형식 + 오탐 유발 케이스 혼합)
- [x] 정답 레이블(ground truth) 정의
- [x] precision / recall / F1 자동 측정 스크립트 + 오분류·룰 커버리지·히스토리 시나리오
- [x] gitleaks와 동일 코퍼스로 비교, 결과표를 README에 게시
- [ ] trufflehog 비교 (미설치)
- [ ] 제3자 코퍼스로 재측정 — 현재 수치의 편향을 없애는 유일한 방법

gitleaks v8.30.1 비교까지 마쳤다 (동일 코퍼스에서 F1 1.000 vs 0.875).
다만 코퍼스를 직접 만들고 거기에 맞춰 룰을 고쳤으므로 **"우수하다"의 근거로는 부족하다.**
코퍼스에 넣을 서비스도 내 룰셋 위주로 골랐다. 공정한 비교에는 제3자 코퍼스가 필요하다.

보완으로 실제 오픈소스 레포 5개(13,294 커밋)를 스캔해 오탐률을 실측했다.
오탐 원인은 2가지뿐이었으나, 이 방법으로는 **미탐을 측정할 수 없다**
(해당 레포들에 진짜 유출이 없기 때문). 자세한 내용은 [eval/README.md](../eval/README.md).

#### 3. baseline 파일 — 완료

이미 시크릿이 많은 레포에 도입하려면 **필수 기능**이었다. 도입 첫날 수십 건이
쏟아지고, 그 뒤로 새로 들어온 것과 원래 있던 것을 구분할 방법이 없으면 CI가
계속 빨간 상태로 남아 아무도 안 보게 된다.

- [x] `--baseline-out <path>` — 현재 탐지 결과를 baseline으로 저장
- [x] `--baseline <path>` — baseline에 있는 시크릿은 결과·검증 대상에서 제외,
      새로 생긴 것만 보고. 로드 실패 시 종료 코드 2 + `--baseline-out`으로
      먼저 만들라는 안내 메시지
- [x] 식별은 (파일 경로, 룰 ID, 시크릿 값의 SHA-256) 조합. **원본 값은 저장하지
      않는다** — baseline은 커밋되는 파일이라 원본을 넣으면 그 자체가 유출 경로가
      된다. 라인 번호는 식별에 쓰지 않아 위쪽 줄 추가/삭제나 새 커밋에도 흔들리지 않음
- [x] baseline 억제는 검증(`--validate`)보다 먼저 적용 — 이미 아는 시크릿까지
      매번 발급처에 물어보면 네트워크 호출과 레이트리밋을 의미 없이 소모하므로
- [x] `--baseline`과 `--baseline-out` 동시 사용은 명시적으로 거부 (의미가 겹쳐 혼란만 생김)
- [x] 기본 룰셋의 `exclude_paths`에 `secrethound-baseline.json` 추가 — 안 하면
      baseline 파일이 스캔 대상 안에 있을 때 자기 자신도 매번 다시 스캔됨
      (실측 중 실제로 발견한 부작용)

#### 4. 병렬 스캔 + 대형 레포 성능 수치 — 완료

기획서에 **"Go를 고른 이유가 goroutine"** 이라고 적어놨는데 구현은 전부 순차 처리였다.
문서와 구현이 어긋난 상태를 메우는 게 목적이었지, 실제로 느려서 고친 것은 아니다
(착수 전 실측에서도 express 5,678커밋을 1.4초에 처리하고 있었다).

- [x] `internal/scanner/parallel.go` — 정규식 매칭(29개 룰)을 goroutine 워커 풀에
      분산. 수집(파일 순회)과 히스토리 파싱(`git log` 출력 해석)은 그대로 한
      goroutine이 순서대로 처리하고, 그 결과로 나온 "파일 하나" 또는 "히스토리
      한 줄" 단위의 매칭만 워커에 맡긴다 — git log 파싱은 상태를 유지하며 diff
      스트림을 해석하는 작업이라 병렬화하면 정확성을 해칠 위험이 이득보다 크다고 판단
- [x] `--workers` 플래그 추가, 기본값 0(= `runtime.NumCPU()`)
- [x] 채널 기반 파이프라인(잡 채널 → 워커 N개 → 결과 채널 → 단일 수집 goroutine)으로
      뮤텍스 없이 데이터 경합을 피함. CI에 `go test -race ./internal/scanner/...`
      추가 (로컬 Windows 환경엔 cgo 툴체인이 없어 `-race`를 못 돌림)
- [x] 완료 순서가 뒤섞여도 결과가 항상 같은지 확인하는 결정성 테스트 추가
      (`TestParallelScanIsDeterministic`, 5회 반복 스캔 비교)
- [x] 실제 레포로 before/after 측정 (16코어, express.js 5,678커밋/210파일)

**측정 중 겪은 두 가지 — 둘 다 결과에 영향을 준 실수였다.**

1. **채널 버퍼 크기.** 처음엔 버퍼를 `workers*4`로 뒀는데, `workers=1`일 때
   버퍼가 4밖에 안 돼 히스토리 스캔(추가된 줄마다 잡 하나, express 기준
   115,806줄)에서 producer/consumer가 계속 서로를 깨워야 했다. 그 결과
   `workers=1`이 병렬화 이전 순차 구현(1,417ms)보다 오히려 느린 4,067ms가
   나왔다. 버퍼를 4,096으로 고정 크기로 늘리자 1,348ms로 정상화됐다.
2. **OS 파일 캐시 워밍업.** 워킹트리 스캔을 `workers=1` 먼저, `workers=0`
   나중에 측정했더니 순서 효과만으로 최대 50배 차이가 나왔다(캐시가 데워진
   쪽이 항상 빠름). 순서를 바꾸고 반복 측정해 실제 차이를 다시 쟀다.

**최종 결과 (캐시 워밍업 후 반복 측정, 3회 이상 평균)**

| 단계 | workers=1 | 자동(16코어) | 비고 |
|---|---:|---:|---:|
| 파일 트리 스캔 (express, 210파일) | ~26ms | ~11ms | 약 2배. 파일 읽기(I/O)가 겹쳐 도는 효과. 절대 시간은 원래 작음 |
| 히스토리 스캔 (express, 5,678커밋) | ~1,278ms | ~1,300ms | **차이 없음.** `git log` 서브프로세스 실행+출력 파싱이 지배적 비용이라 그 뒤 정규식 매칭을 병렬화해도 전체 시간에 거의 영향 없음 |

**정직하게 남기는 결론**: 이 기능은 "느려서 고친 것"이 아니라 "기획 문서와
구현의 불일치를 메운 것"이며, 측정해보니 그 판단이 더 뚜렷해졌다 — 정규식
매칭은 애초에 병목이 아니었고, `git log` 자체가 병목이라는 게 더 정확한
진단이다. 히스토리 스캔의 실질적인 개선이 필요해지면, 워커 풀보다 git 호출
방식 자체(예: 파일별 병렬 `git log`)를 재검토하는 쪽이 맞을 것이다.

**후속 검증 (2026-09-08)**: 위 표의 "약 2배"는 express.js를 1회 수동 클론해 잰
값이라 재현 가능한 형태로 남아있지 않았다. `internal/scanner/bench_test.go`에
저장소 밖 의존 없는 벤치마크를 추가해 다시 재봤더니, CPU 부하만 있는 조건(같은
임시 디렉토리를 반복 재사용해 파일 캐시가 데워진 상태)에서는 이득이 10~15%로
줄었다. 즉 원래 관찰한 "약 2배"는 CPU 병렬화 자체가 아니라 **콜드 캐시에서
한 파일을 매칭하는 동안 다음 파일을 디스크에서 읽어오는 I/O 오버랩** 효과였을
가능성이 크다 — 이 벤치마크 방식으로는 그 조건을 재현하지 못한다. 두 수치가
모순되는 게 아니라 서로 다른 조건(콜드 캐시 vs 웜 캐시)을 잰 것으로 해석하는
쪽이 맞다. 상세 표와 근거는 [평가지표.md](./평가지표.md)의 12번 항목 참조.

#### 5. remediation 안내 — 완료

탐지에서 끝나지 않고 대응까지 연결하는 것이 목적이다.

- [x] 키 종류별 폐기(revoke) 절차 안내 문구를 룰셋에 포함 — `config.Rule.Remediation`
      필드 추가, `rules/default.yaml`의 29개 룰 전부에 발급처별 절차(위치, URL,
      재발급 필요 여부)를 채움. `finding.Finding.Remediation`으로 그대로 실려
      JSON 리포트에도 노출됨
- [x] 히스토리에서 발견된 경우 `git filter-repo` 제거 명령 생성 —
      `finding.Finding.HistoryCleanup`, `reporter.Build()`가 `Commit != ""`인
      finding에만 채움 (`git filter-repo --path '<path>' --invert-paths`)
- [x] "키를 지우기 전에 반드시 폐기부터" 경고 — 텍스트 리포트 맨 아래
      "대응 방법" 절에서 히스토리 발견이 하나라도 있으면 항상 노출. 기존에
      `--validate`가 살아있는 키를 찾았을 때만 뜨던 경고와 달리, 검증 여부와
      무관하게 뜬다 — 폐기 전 히스토리 삭제는 검증 여부와 상관없이 위험하기 때문

**설계 결정 — 원본 값 대신 파일 단위 제거를 기본으로 안내**: `git filter-repo
--replace-text`로 정확한 값만 지우는 방법도 있었지만, 그러려면 원본 시크릿 값이
명령에 그대로 들어가야 한다. 리포트에는 마스킹된 값(`Masked`)만 싣고 원본은
직렬화하지 않는 기존 설계(`Finding.Secret`은 `json:"-"`)와 정면으로 충돌해서
채택하지 않았다. 대신 파일 전체를 히스토리에서 제거하는 `--invert-paths`를
기본으로 안내하고, 값만 지우고 싶은 사용자는 직접 `--replace-text` 규칙을
구성하도록 안내 문구만 덧붙였다.

**같은 룰·같은 파일은 한 번만**: 히스토리 스캔은 같은 시크릿이 여러 커밋에
걸쳐 나오는데, `filter/group.go`가 이미 파일+값 기준으로 묶어주므로 그 결과를
그대로 쓰면 룰별 안내 문구와 파일별 filter-repo 명령이 자연히 한 번씩만 나온다
— 별도 중복 제거 로직을 추가로 만들 필요가 없었다.

실제 레포(임시 스캐폴딩)로 워킹트리 전용/히스토리 포함 두 경로를 모두 스모크
테스트했고, 회귀 방지를 위해 `internal/reporter/reporter_test.go`에 단위
테스트 3개(룰당 한 번만 표시, 히스토리 발견 시에만 정리 명령 생성, 안내할 내용이
없으면 절 자체가 안 나옴) 추가. `go test ./...`, `go run ./eval`
(F1 1.000·커버리지 29/29 유지), `go run ./eval -history` 전부 통과 확인.

## generic-api-key 미탐 두 가지 수정 (2026-09-09)

"룰에 없는 서비스도 대부분 잡히는가"를 실측하려고 룰에 없는 국내외 서비스 27개로
프로브 파일을 만들어 스캔했더니 **0/27**이 나왔다. 그중 일부는 `generic-api-key`가
잡았어야 하는데 두 가지 결함 때문에 놓치고 있었다.

**1. `\b`는 `_` 앞에서 성립하지 않는다.** [naver-openapi-client-secret]에서 이미
한 번 걸렸던 것과 같은 함정을 `generic-api-key` 자신도 갖고 있었다.
`\b(api[_\-]?key|...)`의 선행 `\b`가 `DD_API_KEY`, `GH_ACCESS_TOKEN`,
`DB_PASSWORD`처럼 **접두사가 붙은 환경변수 이름을 전부 놓쳤다.** 이 관례가
국내외를 막론하고 가장 흔한 네이밍이라는 걸 생각하면 파급이 컸다. 선행 `\b`를
제거했다(후행 `\b`는 "apikeyword" 같은 부분일치 오검출을 막아주므로 유지).

같은 수정을 `aws-secret-access-key-bare`에도 시도했다가 되돌렸다. 이 룰의
실제 사용례(Terraform HCL의 `secret_key = "..."`, 줄 앞에 다른 단어 없음)는
선행 `\b`가 있어도 이미 문제없이 잡히는데, 없애면 `NCP_SECRET_KEY=...`의
"SECRET_KEY" 부분까지 이 룰이 가로채 `ncp-secret-key`(critical)가 잡아야 할
값을 `aws-secret-access-key-bare`(critical)로 오분류했다. 두 룰 다 정의 순서상
aws 쪽이 앞이라 Dedupe가 그쪽을 남겼다. 값 형식(40자 base64 계열)이 겹치는
두 국가/서비스 룰이 공존할 때는 이렇게 한쪽만 고치면 다른 쪽이 깨질 수 있다는
걸 보여준 사례라 기록해 둔다.

**2. 엔트로피 게이트 3.5가 정상 32자리 hex 키의 약 19%를 떨어뜨린다.** 32자리
hex의 이론적 최대 엔트로피는 4.0이라 여유가 크지 않다. 난수 2만 개로 측정한
탈락률: hex 16자 90.1%, hex 20자 77.7%, **hex 32자 18.6%**, hex 40자 4.9%.
32자리 hex는 Datadog·Algolia 같은 서비스가 실제로 쓰는 형식이다. 게이트를
3.0으로 낮췄다(하루 전 카카오·NCP·포트원 룰에 적용한 것과 같은 값).

**수정 후 재측정**: 접두사 붙은 이름 11종(위 케이스 포함) 전부 탐지로 전환.
회귀 코퍼스에 `config/prefixed_env_vars.env`로 고정(접두사 이름 3건 + 엔트로피
경계값 1건). `go run ./eval` — F1 1.000 유지(코퍼스 49건, 오탐 0).

**남은 미탐**: 27개 프로브 중 4/27만 회복됐다. 나머지는 `generic-api-key`의
키워드 목록 자체가 `token`/`secret`을 단독으로는 포함하지 않아서다(반드시
`access_token`/`auth_token`/`secret_key`/`client_secret` 형태여야 함). Okta의
`API_TOKEN`, Plaid의 `SECRET`, Notion·Vercel·HubSpot 등의 단독 `TOKEN`이 이
경계 밖이다. 의도적으로 좁힌 스코프라 이번엔 건드리지 않았다 — 넓히면 CSRF
토큰·세션 ID 같은 비시크릿 필드까지 걸릴 위험이 있어, 코퍼스에 그런 트랩
케이스를 먼저 마련한 뒤 별도로 다뤄야 한다.

## 국내 서비스 룰셋 — 완료 (2026-09-08)

국내 서비스 키가 하나도 안 잡히는 상태였다. 공공데이터포털 `serviceKey`를 예로 들면,
전용 룰이 없는 데다 `generic-api-key`의 키워드 목록(`api_key|apikey|secret_key|...`)에
`servicekey`가 없고 값이 따옴표로 감싸져 있어야 해서, URL 쿼리스트링에 박힌 형태는
구조적으로 못 잡았다. 엔트로피 단독 탐지가 있었다면 걸렸겠지만 그건 MVP에서 뺀 기능이다
(위 "Entropy Analyzer" 절).

추가한 룰 8개:

| 룰 | 심각도 | 앵커 |
|---|:---:|---|
| `kr-public-data-service-key` | high | 이름 (`serviceKey`) |
| `kakao-rest-api-key` | high | 접두사 (`KakaoAK`) + 이름 |
| `kakao-admin-key` | critical | 이름 |
| `naver-openapi-client-secret` | high | 이름 / 헤더 |
| `ncp-access-key-id` | high | 이름 / 헤더 |
| `ncp-secret-key` | critical | 이름 / 헤더 |
| `toss-payments-secret-key` | critical | 접두사 (`live_sk_`) |
| `portone-api-secret` | high | 이름 |

### 왜 대부분을 이름 앵커로 잡았나

해외 서비스 룰이 접두사 기반으로 잘 동작하는 건 AWS `AKIA`, Stripe `sk_live_`처럼
발급처가 형식을 공식 문서에 못박아 뒀기 때문이다. 국내 서비스는 그런 곳이 드물다.
카카오 REST API 키(32자리 hex)와 토스페이먼츠 시크릿 키(`test|live_(g)sk_`) 정도만
확인됐고, 네이버 Client Secret이나 NCP 키의 자릿수는 공식 문서에 없다.

자릿수를 추측해서 접두사 룰을 쓰면 추측이 틀렸을 때 **조용히 아무것도 안 잡힌다** —
탐지기의 가장 나쁜 고장 방식이다(검증기 셀프체크를 만든 이유와 같다). 반면 변수·헤더
이름은 그 서비스를 쓰는 코드라면 반드시 나오고 바뀔 일도 없다. 그래서 불확실성을
미탐이 아니라 "값이 이름과 다른 줄에 있으면 못 잡는다"는 쪽으로 넘겼다. 그 경우는
Twilio에서와 마찬가지로 `generic-api-key`가 medium으로 받는다.

### 작업 중 걸린 것

**`\b`는 `_` 앞에서 성립하지 않는다.** 처음엔 `\bnaver[_\-.]?client[_\-.]?secret\b`로
썼는데, `_`가 단어 문자라서 `NEXT_PUBLIC_NAVER_CLIENT_SECRET` 같은 이름을 통째로
놓친다. Next.js 프로젝트에서 흔한 표기라 앞쪽 `\b`를 전부 걷어냈다. 코퍼스에 이 표기를
trap으로 넣어 회귀를 막아 둔다.

같은 성질이 반대로 도움이 된 곳도 있다. `NCP_SECRET_KEY=`는 `SECRET` 앞이 `_`라
기존 `aws-secret-access-key-bare`의 `\bsecret[_\-. ]?key\b`에 걸리지 않는다. 값이
40자 base64라 안 그랬으면 NCP 키가 AWS 키로 오분류될 뻔했다(Dedupe는 심각도가 같으면
먼저 정의된 룰을 남기므로 AWS가 이겼을 것이다).

**엔트로피 게이트를 너무 높게 잡으면 미탐이 된다.** 포트원 `imp_key`는 숫자로만
이뤄져 있어 이론상 최대 엔트로피가 log2(10)≈3.32다. 처음 잡은 3.5로는 정상 키가
영영 안 잡힌다. 국내 룰의 게이트는 "가장 짧고 가장 단조로운 정상 키도 통과하는 값"으로
다시 잡았다(공공데이터 3.5 / NCP·포트원 3.0 / 네이버 2.5). 플레이스홀더 거르기는
어차피 `placeholder-value`·`repeated-chars` 감점이 담당한다.

### 공개값으로 판단해 뺀 것

기존 기준(Stripe publishable key, Google OAuth Client ID)을 그대로 적용했다.

- 카카오 JavaScript 키 — REST API 키와 같은 32자리 hex라 값만으로는 구분되지 않는다.
  그래서 `KakaoAK` 헤더와 `kakao_rest_api_key` 변수명 두 문맥에서만 잡고, 값 단독
  매칭은 하지 않는다. JavaScript 키는 어느 쪽에도 등장하지 않는다.
- 토스페이먼츠 클라이언트 키(`test|live_(g)ck_`), 네이버 Client ID,
  NCP `x-ncp-apigw-api-key-id`, 포트원 가맹점 식별코드(`imp########`)

토스페이먼츠 **테스트** 시크릿 키(`test_sk_`)는 정규식에는 걸리지만 값에 `test`가
들어가 `placeholder-value` 감점(-60)으로 리포트에서 빠진다. 실제 결제가 불가능해
유출 피해가 없다는 판단이고, 코퍼스가 Stripe 테스트 키를 trap으로 처리한 것과 같다.

### 남은 것

국내 8개 룰에는 **검증기가 없다.** 검증기는 발급처에 실제 요청을 보내 401/2xx를 보는
구조라, 각 서비스의 인증 실패 응답 형태를 확인해야 붙일 수 있다. 검증기 커버리지는
17/29에서 17/37로 분모만 늘었다 — 지표에 그대로 적어 둔다.

## 웹 대시보드 — 완료 (스코프 축소)

기획서의 선택 항목(v0.4)이며, 위 "확장 로드맵(차별화 근거)"과는 목적이 다르다 —
차별화 주장을 증명하는 게 아니라 이미 만든 결과를 보기 쉽게 보여주는 게 목적이다.

기획서는 웹 대시보드를 "TBD"로 남겨뒀다 — 프레임워크도 UI 스펙도 정해진 게
없었다. 처음 잡았던 추정(2~3일)은 "서버 + 실시간 스캔 트리거"까지 포함할 경우의
숫자였는데, 실제로 필요한 건 그보다 훨씬 좁았다: **이미 고정된 JSON 스키마
(`reporter.Build`)가 있으니, 그걸 읽어서 보여주기만 하면 된다.**

그래서 스코프를 의도적으로 좁혔다 — 빌드 도구도, 서버도, 프레임워크도 없는
**정적 HTML 파일 한 장**. `secrethound scan --format json --output report.json`
으로 만든 파일을 브라우저에 드래그하면 끝나는 구조.

- [x] `web/dashboard.html` — 순수 HTML/CSS/JS, 외부 의존성 없음(CDN·빌드 없음)
- [x] 파일 선택 / 드래그앤드롭으로 JSON 리포트 로드 (서버 불필요, `file://` 로
      직접 열어도 동작 — `fetch`가 아니라 `FileReader`를 쓴 이유)
- [x] 요약 카드(탐지·파일·커밋·오탐 필터·baseline·소요시간), 심각도 분포
- [x] 검증 요약 배너 — 살아있는 키가 있으면 경고, 오프라인이면 별도 안내
- [x] "대응 방법" 절 — CLI의 `reporter.writeRemediation`과 같은 로직(룰당·
      파일당 한 번만)을 JS로 재구현. `git filter-repo` 명령에 클립보드 복사 버튼 추가
- [x] 심각도 필터 칩, 경로/룰 검색, "히스토리 발견만" 토글, 컬럼 정렬
- [x] 행 클릭 시 상세 패널(설명·태그·엔트로피·검증 사유·개별 remediation) 펼침
- [x] 모든 동적 텍스트를 이스케이프 처리 — 리포트에 담긴 파일 경로·설명은
      스캔 대상 레포에서 온 신뢰할 수 없는 문자열이라 `innerHTML`에 직접 넣지 않음

**검증 방법**: Playwright(로컬에 이미 캐시된 Chromium)로 실제 브라우저에 합성
리포트를 로드해 확인 — 파일 업로드, 테이블 렌더링, 요약 카드, 대응 방법 절
표시/복사 버튼, 심각도 필터, 검색, 행 상세 펼침, 검증 배지(유효/폐기됨/검증불가
3색 구분) 전부 스크린샷으로 눈으로 확인. 콘솔 에러 없음.

**뒤로 미룬 것**: 서버 연동(레포 URL 입력 → 자동 스캔), 여러 리포트 비교(추세),
페이지네이션(현재는 findings 전체를 한 번에 렌더링 — 리포트가 수천 건 단위로
커지면 재검토 필요). 지금 스코프에서는 필요 없어서 만들지 않았다.

## 완료된 작업
- [x] 기술 스택 확정 → **Go**
- [x] 탐지 룰셋 정의 → `rules/default.yaml` (현재 29개 룰)
- [x] 프로젝트 구조 세팅 → `go mod init`, cobra CLI 뼈대, 룰셋 로더
- [x] Repo Collector → 파일 순회, 제외 규칙, 바이너리 판별, 심볼릭 링크 스킵
- [x] Detection Engine → `Finding` 구조체, 라인 단위 매칭, 키워드 프리필터, 엔트로피 게이트, 마스킹
- [x] `scan` 커맨드에 수집기 + 탐지기 연결 (탐지 시 종료 코드 1)
- [x] FP Filter → 신뢰도 감점(경로/값 대상 구분), builtin 규칙, allowlist, 임계값 컷
- [x] Reporter → 심각도순 정렬, 요약 통계, 텍스트/JSON 출력, 색상 처리
- [x] CLI 플래그 → `--rules` `--format` `--output` `--no-color` `--exit-code`,
      종료 코드 분리(0 정상 / 1 탐지 / 2 실행 실패)
- [x] Git 히스토리 스캔 → `git log -p` 스트리밍 파싱, 커밋 메타데이터 기록, `--history` 플래그
- [x] 리포지토리 정리 (`9fd95b8`) → `internal/collector/history.go`, `history_test.go`,
      `internal/filter/`, `internal/reporter/` 커밋
- [x] README → 문제 제기, 설치·사용법, 플래그/종료 코드, 동작 방식, 룰셋 작성법, JSON 스키마
- [x] LICENSE (MIT) 추가
- [x] 평가 하네스 → 레이블 코퍼스, precision/recall/F1, 오분류 집계, 룰 커버리지,
      히스토리 시나리오 평가
- [x] 하네스가 찾아낸 결함 수정 → 오탐 4건, 미탐 5건, 심각도 오분류 1건,
      도달 불가능한 룰 1개 제거, 정답 레이블 오류 1건 정정
- [x] gitleaks v8.30.1 비교 측정 (동일 코퍼스 F1 1.000 vs 0.875, 편향 명시)
- [x] GitHub Actions → `action.yml`, CI 워크플로, `--report` 플래그
- [x] 실제 오픈소스 레포 5개(13,294 커밋) 스캔으로 과적합 검증
- [x] 히스토리 중복 보고 정리 → 같은 파일의 같은 값을 최초 유입 하나로 묶음
      (Flask 기준 12행 → 3행)
- [x] `.gitattributes` → Windows CRLF 체크아웃으로 gofmt 검사가 무력화되던 문제 해결
- [x] GitHub 원격 푸시 (push protection이 코퍼스의 가짜 키 14건을 차단해 테스트 데이터로 허용 처리)
- [x] 키 유효성 검증 → `internal/validator/`, `--validate` / `--validate-timeout` 플래그,
      14개 룰 대응(AWS STS·GitHub·GitLab·Slack·Slack Webhook·Stripe·OpenAI·Anthropic·
      SendGrid·Mailgun·npm·Discord·Telegram·Heroku), AWS SigV4 직접 구현,
      오프라인 회로 차단·레이트리밋 재시도·에러 문자열 redact
- [x] 검증기 셀프체크 → `secrethound selfcheck` 커맨드, 발급처별 실제 거부 응답
      시그니처(상태코드+본문 표식) 확보, 시그니처 불일치 시 `검증불가`로 강등,
      매주 월요일 CI 자동 실행(`selfcheck.yml`). AWS가 401이 아닌 403을 준다는
      사실을 이 과정에서 실측으로 발견
- [x] 검증기 성공 경로 확인 완료 (2026-09-08) → 무료 발급 6곳(GitHub·GitLab·Slack·
      Discord·Telegram·npm) 전부 실제 키로 `유효` 판정 확인 후 즉시 폐기.
      GitLab은 신형식(마침표 포함) 미탐지 룰 버그로 확인되어 수정,
      Slack·npm은 처음에 잘못된 값을 복사한 것으로 판명되어 재발급 후 확인
- [x] GitLab PAT 신형식 룰 수정 → 마침표 포함 라우터블 토큰 형식 지원,
      eval 코퍼스에 회귀 케이스 추가 (`config/services.env`)
- [x] baseline 파일 → `internal/baseline/`, `--baseline` / `--baseline-out` 플래그,
      경로+룰+값해시 기반 지문(라인 번호 무관), 원본 값 미저장, 검증 전 단계에서
      억제해 네트워크 호출 절약, GitHub Actions `baseline-path` 입력 추가
- [x] 병렬 스캔 → `internal/scanner/parallel.go`, `--workers` 플래그, 채널 기반
      워커 풀(뮤텍스 없음), 결정성 테스트, CI `-race` 검사 추가. 실측 결과
      파일 트리 스캔은 약 2배 개선, 히스토리 스캔은 `git log` 자체가 병목이라
      효과 미미 — 정직하게 기록
- [x] remediation 안내 → `config.Rule.Remediation` / `finding.Finding.Remediation`
      (룰별 폐기 절차, 29개 룰 전부), `finding.Finding.HistoryCleanup`
      (히스토리 발견 시 `git filter-repo` 제거 명령), 텍스트 리포트 "대응 방법"
      절에 룰당·파일당 한 번씩만 표시. 원본 값을 리포트에 싣지 않는 기존 설계와
      충돌해 `--replace-text`(정확한 값 치환) 대신 파일 단위 제거(`--invert-paths`)를
      기본으로 채택
- [x] 웹 대시보드 → `web/dashboard.html`, 빌드·서버·외부 의존성 없는 정적 파일
      한 장. JSON 리포트를 드래그앤드롭/파일선택으로 로드해 요약 카드·검증
      배너·"대응 방법" 절(CLI와 동일 로직)·필터/검색/정렬 가능한 테이블로 렌더링.
      Playwright로 실제 Chromium에서 렌더링·상호작용 확인. 기획서의 "TBD" 스코프를
      "JSON을 읽어 보여주기"로 의도적으로 좁힘 (서버·실시간 스캔 트리거는 보류)
- [x] 평가 지표 문서화 → [평가지표.md](./평가지표.md) — 목표/실측/달성 여부를 항목별
      표로 정리. 작성 과정에서 `internal/scanner/bench_test.go`(저장소 밖 레포에
      의존하지 않는 재현 가능한 성능 벤치마크)를 새로 추가했고, 이걸로 재측정하다가
      기존 "파일 트리 스캔 약 2배" 기록이 CPU 부하 조건에서는 재현되지 않는다는 걸
      발견해 원인(콜드 캐시 I/O 오버랩 가능성)까지 추적해 기록함

- [x] 일반 사용자 진입점 (2026-09-08 피드백 반영) → 피드백은 "타겟이 일반인인데
      Go를 깔아야 하고, 터미널을 써야 하고, 결과를 보려면 JSON을 받아 대시보드에
      업로드해야 한다"는 것이었다. 네 갈래로 나눠 처리했다.
  - **리포트 형식** → `internal/reporter/markdown.go`, `html.go`, `open.go`.
        `--format md|html` 과 `--open` 추가. html은 `web/dashboard.html` 을 바이너리에
        내장해두고 리포트 JSON을 `<script type="application/json">` 으로 심어 **한 장짜리
        파일**로 내보낸다. JSON 저장 → 대시보드 열기 → 드래그앤드롭의 3단계가 사라졌다.
        대시보드 쪽은 심어진 리포트가 있으면 바로 렌더하고 없으면 기존 드래그앤드롭으로
        동작해, 화면 코드를 두 벌 유지하지 않는다. 주입은 `json.Encoder` 의 기본
        HTML 이스케이프에 의존한다 — 경로에 `</script>` 를 넣어도 페이지가 깨지지
        않는 것을 테스트로 고정
  - **`check` 커맨드** → `cmd/secrethound/check.go`. 히스토리 포함 여부 판단 →
        스캔 → HTML·MD 저장 → 브라우저 열기를 한 번에 한다. `scan` 은 플래그 제어와
        CI(종료 코드 분리)용으로 그대로 두고, `check` 는 탐지해도 0으로 끝난다
  - **더블클릭 실행** → `packaging/windows/` (실행기 `scan.bat`, 바로가기 생성
        `make-shortcut.ps1`, 패키징 `make-zip.ps1`, `사용법.txt`), 아이콘
        `assets/icon.ico` + 생성기 `assets/icongen`, exe 리소스
        `cmd/secrethound/resource_windows_amd64.syso`
  - **릴리스 배포** → `.github/workflows/release.yml`. 태그를 밀면 5개 타깃 바이너리를
        만들어 Releases에 올린다. `action.yml` 도 릴리스 바이너리를 받도록 바꿔
        워크플로에서 `setup-go` 가 필요 없게 했다

      **작업 중에 실측으로 드러난 것들** (전부 추측이 아니라 실행해서 확인했다):

  - `rev-parse --is-inside-work-tree` 는 상위 폴더까지 거슬러 올라가 답한다.
        홈 디렉토리가 git 저장소인 환경에서는 아무 폴더나 "저장소"로 판정되어, 고른
        폴더와 무관한 저장소의 히스토리를 검사하게 된다. `collector.RepoRoot` /
        `IsRepoRoot` 로 **최상위인지**를 따로 확인하도록 고침
  - 위 문제 때문에 `TestWalkHistoryRejectsNonRepo` 가 이 PC에서 **우연히** 통과하고
        있었다 — `t.TempDir()` 이 홈 저장소 아래라 저장소로 판정되고, 그 저장소에
        커밋이 없어 `git log` 가 실패한 것을 "거부됨"으로 읽고 있었다.
        `GIT_CEILING_DIRECTORIES` 로 환경 의존을 끊음
  - 커밋이 하나도 없는 저장소에서 `git log` 는 128로 실패한다. `scan --history` 가
        통째로 실패하던 기존 버그를 "히스토리 0건"으로 고침
  - `chcp` 와 한글이 같은 배치 파일에 있으면 cmd.exe가 파일 읽기 위치를 잃고 다음
        줄들을 중간부터 실행한다. 그래서 `scan.bat` 은 ASCII만 두고, 한글 메시지와
        폴더 선택 창(`check --pick`)을 전부 exe로 옮겼다
  - `Compress-Archive` 는 파일 이름을 UTF-8로 쓰면서 zip의 UTF-8 표시(bit 11)를
        켜지 않는다. Windows 탐색기는 알아서 읽지만 그 표시를 보는 도구는 이름을
        CP949로 읽어 깨진다. `make-zip.ps1` 이 중앙 디렉토리를 따라가며 플래그를 켠다
  - PowerShell 5.1은 BOM 없는 `.ps1` 을 시스템 ANSI로 읽는다. 스크립트 안 한글이
        깨져 실패했고, UTF-8 BOM으로 저장해 해결 (`.gitattributes` 에 기록)
  - `goversioninfo` 는 `StringFileInfo` 필드를 하나라도 빠뜨리면 문자열 블록 전체를
        내보내지 않는다. 아이콘은 붙는데 파일 속성의 제품명·설명이 전부 비는 상태였다
  - **바로가기(.lnk) 방식 자체를 폐기했다.** v0.4.0 을 실제로 배포해 받아보니 zip은
        멀쩡한데(한글 파일명·UTF-8 플래그·exe 모두 정상) 바로가기만 동작하지 않았다.
        `.lnk` 는 **만든 기계의 절대 경로**를 담는다. 러너가 만든 링크의 대상은
        `D:\a\SecretHound\SecretHound\stage\scan.bat` 이고, 사용자 PC에는 그런 경로가
        없다. Windows 가 링크 옆의 파일로 다시 찾아주기를 기대해야 하는데 그게 보장되지
        않는다 — 로컬에서 만든 링크는 재배치가 됐고(3,033바이트) 러너가 만든 것은
        실패했다(1,111바이트). 배포용 zip에 넣기에 구조적으로 부적합한 방식이다.
        **경로 해석이 아예 필요 없는 exe 를 진입점으로 바꿨다**: 탐색기에서 더블클릭한
        경우를 `GetConsoleProcessList` 로 감지해(콘솔에 이 프로세스만 붙어 있으면
        더블클릭이다) 폴더 선택 창을 띄우고, 폴더를 끌어다 놓으면 그 폴더를 검사한다.
        `scan.bat` 과 `make-shortcut.ps1` 은 함께 삭제했다.
        **이 실패는 검증 방식의 빈틈에서 왔다** — 재배치 테스트를 로컬에서 만든 링크로만
        했고, "빌드한 기계와 실행하는 기계가 다르다"는 조건을 재현하지 않았다
  - **바로가기의 한글 이름이 러너에서만 저장 실패했다.** 로그는
        `Unable to save shortcut "...\??? ????.lnk"` 였다. `WScript.Shell` 은 문자열을
        시스템 ANSI 코드페이지로 변환하는데, GitHub 러너는 영문 Windows(CP1252)라
        한글을 표현하지 못해 파일명이 `?` 로 바뀌고 저장이 실패한다. 이 PC는
        CP949(한국어)라 통과했으므로 **로컬에서는 원리상 재현될 수 없는** 버그였다.
        확인 과정에서 설명(툴팁)은 실패 없이 조용히 `???` 로 망가진다는 것도 드러났다
        — 실패하는 쪽보다 이쪽이 더 나쁘다. `IShellLinkW`/`IPersistFile` 을 직접 써서
        UTF-16으로 주고받도록 바꿨다. 검증은 러너를 흉내내기 위해 **CP949로도 표현할 수
        없는 문자**(데바나가리)로 같은 조건을 만들어, 저장 성공·설명 보존·재배치 동작을
        확인했다. 러너와 같은 셸(pwsh 7)을 로컬에 설치해 워크플로 스텝을 그대로 돌린
        것도 이때부터다
  - **패키징 스크립트가 CI에서만 죽었다.** 첫 워크플로 실행(`workflow_dispatch`)에서
        windows 잡의 "패키지 구성"만 실패했다. .NET API는 상대 경로를 PowerShell의
        현재 위치가 아니라 **프로세스의 작업 디렉토리** 기준으로 푸는데, 이 둘이 CI에서
        갈린다. 로컬에서는 절대 경로로만 호출해봐서 드러나지 않았다. `make-zip.ps1` 이
        받은 경로를 절대 경로로 바꾼 뒤 .NET에 넘기도록 고쳤고, 두 경로가 다른 상태를
        만들어 재현·검증했다
  - **한글 파일명이 히스토리에서 깨지고 있었다.** 완성한 도구로 이 저장소 자신을
        검사해 보고 발견했다. `core.quotepath` 기본값 때문에 `git log -p` 가 비ASCII
        경로를 `"b/docs/\354\232\251..."` 처럼 8진수로 이스케이프해 내보내는데, 파서가
        `b/` 접두사만 벗기고 있었다. 그래서 히스토리 쪽 경로가 워킹트리 쪽과 달라져
        **같은 시크릿이 두 건으로 갈라져** 보고됐고, 더 나쁜 것은 리포트에 실리는
        `git filter-repo --path` 명령의 경로도 그 깨진 값이라 **그대로 실행하면 아무
        파일도 지우지 못한다**는 점이었다. `-c core.quotepath=false` 로 원인을 없애고,
        따옴표로 감싸이는 나머지 경우를 위해 `unquotePath` 로 C 이스케이프를 푼다.
        회귀 테스트를 붙이는 과정에서 **파일명에 공백이 있으면 git이 이름 뒤에 탭을
        붙인다**는 것(unified diff 관례)도 함께 드러나 경로 끝에 탭이 섞이던 것을 고쳤다

> **정정 기록**: "Git 히스토리 스캔"이 한때 문서에 미완료로 적혀 있었으나 실제로는 구현 완료 상태였다.
> 커밋되지 않아 untracked였을 뿐이며, `9fd95b8`에서 커밋됨.
